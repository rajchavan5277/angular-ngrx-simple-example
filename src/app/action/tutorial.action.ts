import { Injectable } from '@angular/core';
import { Action } from '@ngrx/store';
import { Tutorial } from '../model/tutorial.model';

// Create a const which will be used in action and reducer for.
